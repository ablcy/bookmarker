services:
  - type: web
    name: chat-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    repo: https://github.com/ablcy/chat
    branch: master
    rootDir: .
